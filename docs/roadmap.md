# Роадмап maestro-agent (с 4.6.1)

Основание: 18 открытых пунктов `TODO.md` + ревью opus (2026-09-22, вердикт «Revise» —
принят, правки ниже). Порядок: надёжность памяти → качество пайплайна → доки → чистка
(major) → рост (epic) → эксперименты (research).

## Interim (немедленно, 0 кода)

- **M1.** Регулярный `memory_export` в dogfood — снимает остроту «single point of loss»
  до реализации #101 (инструмент уже есть; double-masking в `memory_import` реализован).

## Волна 1 — Надёжность памяти + гигиена процессов (4.7.x)
> Волна 1 закрыта релизом **4.8.0** (метка «4.7.x» — историческая; #113 — новая фича → minor-бамп 4.7.1 → 4.8.0).

Порядок внутри волны: **#101 → #77** (dogfood-репо на sqlite — сценарий #77 для него
не «горит»).

1. **#101** — memory backup/restore (tools/команды + `memory.backup` + how-to-инструкция).
   **ИБ-гейты (C3, до реализации):** повторная маскировка записей при бекапе
   (переиспользовать механику import/double-masking); warn при конфигурации пути
   вне `.gitignore`; раздел в docs «коммитить бекапы только в приватные репо».
   v1 — только sqlite. Interim до релиза — M1.
2. **#77** — деградация при недоступной векторной БД. **Выполнено (4.7.1, 2026-09-24)** —
   дизайн-гейт C4 решён в пользу fail-closed «зафиксировать в сессии, что данные не
   сохранены» (spec 2026-09-23): loud-уведомления (per-session + process-level) +
   восстановление по требованию (`memory_reindex` full-reindex), sqlite-fallback —
   non-goal.
3. **#113** — `@maestro-feedback-report`: режимы `auto/manual/disable` в
   `maestro.json`. **Выполнено (4.8.0, 2026-09-24)** — ключ `feedback_report`
   (дефолт `manual`), плагин-директива в system-контексте (паттерн
   `communication`), шаг 18.5 пайплайна (manual — подсказка / auto —
   авто-сбор без HITL, fail-soft / disable — ничего); spec
   `docs/superpowers/specs/2026-09-24-feedback-report-modes-design.md`.

Параллельно (дёшево, не релизные обязательства):

- **#87 (I2)** — spike: инвентаризация HITL-вопросов (a)/(b)/(c) в SKILL.md против
  нативного question-тула. Если «хватает» — правки промптов в 4.7.x/4.8.x, не мажор.
  Ограничение: интерактивный выбор не ослабляет «подтверждение только явным
  выбором» на закрытом перечне гейтов (security-гейты, 1/1.5, D2/D6/D7, плато).
- **#99 (I4)** — раздел «куда присылать feedback-reports» (подтянут к Волне 1:
  с 4.7.x отчёты могут собираться в auto-режиме).
- **#117 — разбор (I3)** — сценарии использования `sanitizer_whitelist`: есть ли
  латентные дыры. Изменения — в 5.0. Активная проблема → эскалация вне роадмапа.

## Волна 2 — Качество пайплайна (4.8.x)

Порядок: **#109+#115 → #95 → #107** (I1: без baseline-метрик эффект #95 недоказуем).

1. **#109 + #115** — единый блок метрик пайплайна в feedback-report: циклы (в т.ч.
   циклы ревью), токены, длительности (переиспользовать 4.5.0-таймлайн:
   `opencode export` + `timeline.mjs`, 0 LLM — не строить параллельный сбор) +
   методология Effort и факторы влияния.
2. **#95** — сокращение повторных ревью: переформулировка/расширение трактовки задачи
   ревью + параллельное первое ревью на 2 агента (sonnet + opus, только при разных
   моделях). M3: определить tie-breaker при расхождении вердиктов.
3. **#107** — шаг 16: ревью по типу артефакта (код/доки/презентация/изображение);
   требования на тип — в `project-context.md`. M4: привязать к существующим 14
   категориям, не плодить параллельную схему.

## Волна 3 — Документация и сопровождение (4.9.x)

1. **#85** — команда актуализации документации (manual_docs/README/SECURITY/AGENTS
   vs фактическое состояние). M5: автоматизация существующей дисциплины синка
   (правило AGENTS.md, шаги 14/15, `keep-docs-up-to-date.md`), не третий механизм.
2. **#97** — читабельность manual_docs + зафиксировать правила в скиле manual_docs.
3. **#103** — актуальность regression-entries. M6: обновление entries при прогоне
   пайплайна (на шаге ревью), а не отдельной рутиной.
4. **#87 (правки)** — по результату спайка, если не требует breaking.

## 5.0 (major) — только чистка (C1)

- **#91** — убрать back-compat из описаний и `*.sh`; проверить согласованное удаление
  rename-совместимости (maestro-new→setup и т.п.) из тестов/скриптов; migration notes
  в changelog. Не комбинировать с #121 (конфликт зон: оба переписывают
  install/init — регрессии не атрибутируются).
- **#117 — изменения** (по итогам разбора из Волны 1).
- **Хвост #93 «Этап B после V1»** — runtime-верификация merge-семантики нативных
  permissions (внешняя зависимость от opencode; см. SECURITY.md).
- **#87** — только если решение окажется breaking.

> Закрыт (C2): #93 «разбор access_policy» уже реализован (2026-09-16,
> `docs/superpowers/specs/2026-09-16-remove-access-policy-design.md` + regression
> entry, SECURITY.md P6) — TODO закрыть.

## 5.1+ — Epic «Рост» (вне релизных обязательств)

- **#123 → #121** (порядок закреплён, I6): манифест приоритетных технологий
  (вход для `project-context.md`) → пилот миграции существующего проекта.
  Требования: критерии проект-кандидата; правило «пилот не блокирует релиз»;
  security-ревью по SECURITY.md в DoD (копирование проекта в confidential-зону);
  строится на чистой базе после #91.

## Research backlog (эксперименты)

- **#89** — майки-оценки задач (XS–3XL) — питается метриками Волны 2.
- **#119** — оценка квалификации пользователя — чувствительная тема (эффект
  надзора/доверие, M8); «после обсуждения», опционально, не в DoD релиза.

## Непокрытые пункты — статус зафиксирован (I5)

| Пункт TODO | Статус |
|---|---|
| Строка 125 (lite/pro-режимы) | out-of-scope (вывод спеки P1.3) — подтвердить осознанно |
| Строка 137 (гайд GLM/DeepSeek/Qwen) | backlog |
| Строка 154 (реорганизация SKILL.md → `references/*.md`) | согласовать с Волной 2 (контентные правки #95/#107 vs структурная реорганизация) — решение до старта Волны 2 |
| Строка 156 (векторная индексация скилла) | backlog |

## DoD каждой волны (M7)

manual_docs-синк + regression entry + changelog (правила AGENTS.md); для #91 —
добавить migration notes.
