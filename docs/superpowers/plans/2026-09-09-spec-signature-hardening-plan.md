# Plan: Ужесточение обработки подписей spec (внешний fast-track)

Spec: `docs/superpowers/specs/2026-09-09-spec-signature-hardening-design.md` (approve).

**Сервис:** root (authoring-репо maestro). **Тест:** `npm test` (регресс плагина, не меняется).

Суть: подпись spec — provenance-рекомендация, а не гейт допуска. Для всех
7d-входов убрать авто-пропуск 8.6/9 по подписи; 8.6 всегда (кроме HITL-заверения
при валидной `CLEAN`), 9 всегда предлагается через HITL. Планы подписями не
маркируются. Принцип зафиксировать в SECURITY.md (P7) и синхронизировать manual_docs.

## Task 1 — SKILL.md: шаг 7d, блок «проверка подписей» (Р1)

Файл: `skills/maestro/SKILL.md` (~274-284). Заменить блок после «После выбора (d)»:

Сейчас:
```
      После выбора (d) → **проверка подписей** (см. «Подписи spec-файла»):
        - валидная review-подпись → шаги 9/10 пропускаются без вопроса (auto);
        - нет/stale → HITL вариант B: «Уже отревьюен извне?
          (a) пропустить review / (b) прогнать (шаги 9+10)»;
        - валидная sanitize-подпись (`status: CLEAN`) → шаг 8.6 пропускается;
          иначе → шаг 8.6 выполняется.
```
На:
```
      После выбора (d) → **проверка подписей** (см. «Подписи spec-файла»).
      Для всех 7d-входов (включая re-entry spec'ов контура) подпись —
      provenance-рекомендация, НЕ основание авто-пропуска:
        - 8.6 (sanitize) выполняется ВСЕГДА, кроме явного HITL-заверения
          доверия к источнику при валидной (не stale) подписи `status: CLEAN`
          («доверяю источнику, sanitize пропустить»). `FINDINGS_ACCEPTED` →
          8.6 выполняется всегда (правило 4 «Подписи spec-файла» сохраняется).
        - 9 (Spec Review) НЕ пропускается авто по review-подписи; предлагается
          всегда через HITL: (a) пропустить — шаги 9/10 не выполняются, подпись
          не ставится; (b) прогнать — шаги 9+10, при Approve поставить
          review-подпись.
```

## Task 2 — SKILL.md: шаг 8.6, fast-track-заметка (Р1)

Файл: `skills/maestro/SKILL.md` (~341-345). Сейчас:
```
       — **Fast-track (шаг 7d):** если в spec есть валидная sanitize-подпись
         `status: CLEAN` (hash совпадает) → шаг 8.6 пропускается. При
         `FINDINGS_ACCEPTED`, отсутствии или stale-подписи → выполняется.
```
На:
```
       — **Fast-track (шаг 7d):** для всех 7d-входов 8.6 выполняется ВСЕГДА,
         кроме явного HITL-заверения доверия к источнику при валидной (не stale)
         sanitize-подписи `status: CLEAN`. `FINDINGS_ACCEPTED` → 8.6 выполняется
         всегда (правило 4 «Подписи spec-файла»).
```

## Task 3 — SKILL.md: шаг 9, fast-track-ветка (Р1)

Файл: `skills/maestro/SKILL.md` (~380-384). Сейчас:
```
        - **Fast-track (шаг 7d):** валидная review-подпись → шаг пропущен
          (auto). Нет/stale → уже согласовано на шаге 7 (вариант B):
          (a) пропустить — шаги 9/10 не выполняются, подпись не ставится;
          (b) прогнать — выполнить шаг 9 + шаг 10, при Approve поставить
          review-подпись.
```
На:
```
        - **Fast-track (шаг 7d):** review-подпись — provenance-рекомендация,
          НЕ авто-пропуск. Шаг 9 предлагается всегда через HITL:
          (a) пропустить — шаги 9/10 не выполняются, подпись не ставится;
          (b) прогнать — выполнить шаг 9 + шаг 10, при Approve поставить
          review-подпись.
```

## Task 4 — SKILL.md: Security Review, Точка 1 (Р1)

Файл: `skills/maestro/SKILL.md` (~1330-1332). Сейчас:
```
  В fast-track (шаг 7d) пропускается только при валидной sanitize-подписи
  (`status: CLEAN`); после прогона оркестратор штампует подпись
  `<!-- maestro:sanitize -->` (см. «Подписи spec-файла»).
```
На:
```
  В fast-track (шаг 7d) для всех 7d-входов выполняется всегда, кроме явного
  HITL-заверения доверия к источнику при валидной (не stale) sanitize-подписи
  (`status: CLEAN`); после прогона оркестратор штампует подпись
  `<!-- maestro:sanitize -->` (см. «Подписи spec-файла»).
```

## Task 5 — SKILL.md: «Подписи spec-файла» (Р2 + Р3)

Файл: `skills/maestro/SKILL.md` (~1436-1488).

5a. Обновить вводный абзац: «Используются fast-track (шаг 7d) для детекта
«внешний spec уже отревьюен/санизирован»» → подчеркнуть рекомендательный
характер (provenance для HITL-решений, не авто-пропуск).

5b. Добавить правила 9 и 10:
```
9. **Provenance-рекомендация, не гейт допуска:** отсутствие подписи НЕ
   блокирует вхождение spec/плана в работу. Для всех 7d-входов подпись не даёт
   авто-пропуска шагов (см. шаг 7d): 8.6 выполняется всегда (кроме HITL-
   заверения при валидной `CLEAN`), 9 предлагается всегда. Подпись — только
   рекомендация для HITL-решений.
10. **Планы не маркируются подписями:** у плана (шаг 11) нет подписи; его гейт —
    обязательный HITL-аппрув (шаг 12). Вводить `<!-- maestro:plan -->` избыточно;
    контент плана санируется при диспатче на implementer (Точка 2).
```

## Task 6 — SECURITY.md: P7 (Р4)

Файл: `SECURITY.md`, §3 «Требования ИБ», после P6 (~48). Добавить:
```
- **P7.** Для fast-track-входов (шаг 7d) подпись spec (`maestro:sanitize`/
  `maestro:review`) не является основанием авто-пропуска sanitize/review:
  подпись подделываема и доказывает лишь соответствие hash'а содержимому, не
  факт прохождения процесса в контуре. 8.6 для 7d-входов выполняется всегда,
  кроме явного HITL-заверения доверия к источнику при валидной `CLEAN` (правило
  4 «Подписи spec-файла» сохраняется). Статус: инструкция SKILL (не
  enforcement), как P5-гейт.
```
В §4 «Реализованные контрмеры» добавить строку: «Правило подписей (P7): в
fast-track подпись = рекомендация, не авто-пропуск 8.6/9».

## Task 7 — sandbox-checklist C13 (Р1, +M-5)

Файл: `docs/testing/maestro-sandbox-checklist.md` (~51). Обновить C13:
```
| C13 | fast-track re-entry (7d-вход): 8.6 выполняется всегда; 9 предлагается всегда через HITL (a)/(b); HITL-заверение → skip 8.6 только при валидной `CLEAN`; `FINDINGS_ACCEPTED` → 8.6 всегда | ✅ | подпись = рекомендация, не авто-пропуск | |
```

## Task 8 — manual_docs/reference/hitl-gates.md (синк Р1/Р2)

Файл: `manual_docs/reference/hitl-gates.md` (~69-74). Обновить Fast-track bullet:
```
- **Fast-track (шаг 7d):** внешний spec вместо дизайна (шаг 8). Для всех
  7d-входов подпись — рекомендация, не авто-пропуск: sanitize (8.6) выполняется
  всегда (кроме HITL-заверения при валидной `CLEAN`), ревью (9) предлагается
  всегда через HITL — (a) пропустить / (b) прогнать. Отсутствие подписи не
  блокирует вход.
```

## Task 9 — manual_docs/tutorials/run-first-feature.md (синк Р2)

Файл: `manual_docs/tutorials/run-first-feature.md` (~62-63). Заменить:
```
  валидная подпись ревью/санизации в spec пропускает соответствующие гейты.
```
На:
```
  подпись в spec — рекомендация, не авто-пропуск гейтов: sanitize (8.6)
  выполняется всегда (кроме HITL-заверения при валидной `CLEAN`), ревью (9)
  предлагается всегда. Отсутствие подписи не блокирует вход.
```

## Task 10 — manual_docs/explanation/agents-and-trust.md (синк Р4)

Файл: `manual_docs/explanation/agents-and-trust.md` (~248). В контексте
confidential-артефактов (спеки читаются untrusted) добавить предложение:
«Подпись spec — provenance-рекомендация, не основание авто-пропуска 8.6/9
(см. SECURITY.md → P7); защиту обеспечивает очистка (шаг 8.6), а не подпись».

## Task 11 — manual_docs/overview/changelog.md (запись)

Файл: `manual_docs/overview/changelog.md`. Добавить запись о поведении:
«Fast-track-подписи spec — provenance-рекомендация, не авто-пропуск 8.6/9:
для 7d-входов sanitize (8.6) выполняется всегда (кроме HITL-заверения при
валидной CLEAN), ревью (9) предлагается всегда. Планы подписями не
маркируются. SECURITY.md → P7.» — разместить в текущем разделе (top) изменений.

## Task 12 — Верификация (регресс + согласованность)

- `npm test` (плагин; должен остаться зелёным — плагин не менялся).
- `rg` по `skills/maestro/SKILL.md`: не осталось авто-skip формулировок
  «подпись → пропускается (auto)» для 7d-входов.
- `rg` по manual_docs: нет старых формулировок «валидная подпись → гейт
  пропускается».
