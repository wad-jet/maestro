---
name: maestro-feedback-report
description: Сбор фактуры по прошлым процессам maestro для ретроспективы — генерация отчёта в .maestro/feedback-reports/
---

## Гейт 0 — Проверка плагина maestro-bootstrap (обязательный)

**Язык HITL:** русский.

1. **Маркер проекта.** Если в корне проекта есть `maestro.json` — это проект под
   управлением maestro, выполняется проверка плагина (шаг 2). Если `maestro.json`
   НЕТ — проект не под maestro, гейт пропускается, работаем как обычно.

2. **Плагин реально работал.** Открой самый свежий файл
   `.maestro/logs/maestro-bootstrap-<дата>.log` (по имени-дате). Найди строку
   `plugin initialized`. Если есть И её ISO-`ts` не старше 24 часов от текущего
   момента — плагин работает, продолжить работу. Иначе → шаг 3 (стоп).

3. **Жёсткий STOP (без «продолжить»).** Останови работу и покажи HITL:

   > **Плагин `maestro-bootstrap` не подключён или не загружен.**
   > Защита `docs/confidential/**` НЕ действует: confidential-данные могут быть
   > доступны untrusted-агентам и primary-сессии. `access_policy` и sanitizer тоже
   > не работают (все — в плагине `maestro-bootstrap`).
   >
   > Продолжение работы запрещено. Единственный способ продолжить — подключить
   > плагин и перезапустить opencode:
   > ```
   > opencode plugin "maestro-bootstrap@git+https://github.com/wad-jet/maestro.git"
   > # spec добавить в global ~/.config/opencode/opencode.json (реком.) или .opencode/opencode.json
   > ```
   >
   > (a) Подключить плагин и перезапустить opencode — затем повторить команду
   > (c) Отмена / стоп

   Допустимы ТОЛЬКО исходы (a) и (c). Варианта «продолжить как есть» НЕТ.
   При (a): объяснить, что нужно перезапустить opencode и повторить команду,
   НЕ продолжать pipeline в текущей сессии. При (c): завершить работу.

# Maestro Feedback Report

## Overview

Сбор фактуры по прошлым процессам с участием maestro **в текущей сессии** для
последующей ретроспективы (проведение ретроспективы здесь НЕ требуется — только
сбор). Отчёт сохраняется в `.maestro/feedback-reports/report-<Session ID>-<YYYY-MM-DD>.md`.

**Язык:** все HITL-вопросы, варианты и сообщения пользователю — только на русском.

## Шаг 1. Определить текущую сессию

1. Получи `Session ID` текущей сессии через `opencode session list` (текущая —
   самая последняя по времени создания/обновления, либо подтверди у пользователя).
2. Определи текущую дату `YYYY-MM-DD` (формат ISO).
3. Вычисли путь отчёта:
   `.maestro/feedback-reports/report-<Session ID>-<YYYY-MM-DD>.md`.

## Шаг 2. Собрать фактуру из диалога текущей сессии (основной источник)

Проанализируй ход текущей сессии и зафиксируй качественную фактуру:

- Какие фичи/задачи обрабатывались через maestro (или его команды:
  - `@maestro-init`, `@maestro-new`, `@maestro-design`).
- HITL-гейты: какие пройдены, где буксовали, где были отмены/повторы.
- Ключевые решения, шаги, отклонения от плана.
- Стопоры и проблемы, замеченные по ходу.

## Шаг 3. Дополнить данными из логов плагина (автоматически, если есть)

Есть **два** лога плагина в `.maestro/logs/` (JSONL). Они агрегируются по-разному:

### 3a. Bootstrap-лог — по текущей сессии

Проверь `.maestro/logs/maestro-bootstrap-*.log` на наличие записей с полем
`sessionID`, равным текущей сессии. Если записей нет — в отчёте пометь секцию
«нет данных по логам плагина».

Если записи есть — агрегируй:

- **task-диспатчи** (`tool.execute.before`/`after`): количество, средняя/макс
  длительность (`durationMs`), тайтлы — **только количество** (без содержимого).
- **`session.error`** — ошибки сессии.
- **`session.status.retry`** — повторы (attempt, message — напр. rate limit).
- **`tool.execute.after.empty_result`** — пустые результаты сабагентов.
- **`sanitizer.redacted`** — что замаскировано (без содержимого).

### 3b. Аудит-лог — по диапазону дат отчёта (все сессии)

Проверь `.maestro/logs/maestro-audit-*.log` на записи за период отчёта.
**НЕ фильтруй по `sessionID` текущей (primary) сессии**: confidential-события
логируются в дочерних (child) сессиях субагентов и не привязаны к primary.
Агрегируй по диапазону дат:

- **`confidential.access`** — доступ к confidential-путям: количество `allow` и
  `deny` отдельно, имена trusted-агентов (**только имена**, без путей/данных).
- **`access_policy.blocked`** — блокировки доступа (количество + basename файлов).

> **БЕЗОПАСНОСТЬ (SEC-4b):** в отчёт пиши только **агрегаты/количества** и
> обезличенные формулировки. НЕ копируй дословно строки логов, тайтлы субагентов,
> текст ошибок, пути файлов или фрагменты диалога, которые могут содержать
> чувствительные данные. Отчёт — это ретроспективная фактура, а не дамп.

## Шаг 4. Собрать отчёт

Создай каталог `.maestro/feedback-reports/` (если нет) и запиши файл по пути из
Шага 1 со структурой:

```markdown
# Feedback Report — <Session ID> — <дата>

## Метаданные
- Session ID: <id>
- Дата: <YYYY-MM-DD>
- Фичи/задачи: <список>
- Команды maestro: <@maestro-init, @maestro-new, @maestro-design ...>

## Ход процесса
<chronology: шаги, гейты, решения>

## Что было хорошо
<qualitative>

## Что было плохо
<qualitative>

## Проблемы, с которыми столкнулись
### Из диалога
<стопоры, HITL-буксования, отклонения>

### Из логов плагина (bootstrap-лог)
<или «нет данных по логам плагина»>
- task-диспатчи: <кол-во, длительности>
- session.error: <...>
- retry: <...>
- empty_result: <...>
- sanitizer.redacted: <...>

### Из аудит-лога (по диапазону дат)
- confidential.access: <кол-во allow / кол-во deny> + имена trusted-агентов (только имена)
- access_policy.blocked: <кол-во блокировок> + basename файлов

## Критические замечания / рекомендации
<кратко, только если есть выраженные критические моменты>

## Безопасность
> Агрегация событий безопасности по логам плагина и фактам из диалога.
> Детальный контент НЕ приводится (SEC-4b).

### Санитарные события из логов
- sanitizer.redacted: <кол-во замаскированных элементов>
- sanitizer.matched_rules: <уникальные правила, которые сработали — только name, без values>
- confidential.access: <кол-во allow / кол-во deny> + имена trusted-агентов (только имена)
- access_policy.blocked: <кол-во блокировок> + basename файлов (из аудит-лога)

### Безопасность из диалога
<упоминания безопасности, sanitizer, секретов, доступов — только темы, без чувствительных данных>

> Если событий безопасности не обнаружено (sanitizer.redacted = 0, confidential.access
> allow = 0 И deny = 0, access_policy.blocked = 0, из диалога не выявлено):
> «Событий безопасности в текущей сессии не обнаружено.»

## Завершение

- Сообщи: «Отчёт сохранён в `<путь>`».
- **Автокоммит НЕ требуется:** `.maestro/feedback-reports/` — эфемерное (весь `.maestro/` в `.gitignore`, see `skills/maestro-new/SKILL.md`). Коммит отчёта — на усмотрение пользователя.
